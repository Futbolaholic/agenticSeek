repos:
  - repo: local
    hooks:
      - id: trufflehog
        name: TruffleHog
        description: Detect secrets in your data.
        entry: bash -c 'trufflehog -h >/dev/null'
        language: system
        stages: ["commit", "push"]
        pass_filenames: false
